# `components/` (Componentes)

Componentes reutilizáveis da interface.

## O que deve ser programado aqui

- Componentes de UI (botões, inputs, modais) e componentes compostos (ex.: “Resumo do Pedido”).
- (Futuro) Componentes do Shadcn UI e wrappers padronizados.

