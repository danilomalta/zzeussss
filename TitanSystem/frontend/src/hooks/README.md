# `hooks/` (Hooks reutilizáveis)

Hooks React reutilizáveis para encapsular comportamentos.

## O que deve ser programado aqui

- Hooks de acesso a API (ex.: `useProducts`, `useOrders`)
- Hooks de UX (ex.: debounce, teclado, foco, atalhos de PDV)
- Hooks de tema/idioma quando aplicável

