# `public/` (Arquivos estáticos)

Conteúdo estático servido diretamente pelo Vite (sem passar pelo bundler).

## O que deve ficar aqui

- Ícones, imagens públicas, manifest (se aplicável), assets que não precisam de import via JS/TS.

