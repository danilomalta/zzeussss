import React from 'react'
import ReactDOM from 'react-dom/client'
import App from npm run devicePixelRatio // Importa o visual
import './index.css' // Importa o reset básico

// Esta é a linha que manda desenhar na tela:
ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)