import React from 'react';

/*
CHECKOUT / PAGAMENTO PAGE
=========================
Recebe a totalização da venda e gerencia multi-meios de pagamento (Dinheiro, Cartão TEF, PIX).
Ao finalizar com sucesso, chama a API do backend de Invoicing para geração do XML Fiscal.
*/

const Checkout = () => {
  return (
    <div>
      {/* Estrutura futura do Checkout */}
      <h1>Pagamento & Faturamento</h1>
    </div>
  );
};

export default Checkout;
