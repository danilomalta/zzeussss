import React from 'react';

/*
LOGIN PAGE
==========
Ponto de entrada do ecossistema. 
Autentica o usuário globalmente, recupera o JWT (HttpOnly preferencialmente)
e define no Redux/Context a "Loja Ativa" e as permissões de módulo (PDV, B2B, ERP).
*/

const Login = () => {
  return (
    <div>
      {/* Estrutura futura de Login */}
      <h1>Acesso TitanSystem</h1>
    </div>
  );
};

export default Login;
